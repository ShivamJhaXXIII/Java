

public class stringMethods {
    public static void main(String[] args) {
        
        String name = "Shivam Jha";

        // boolean result = name.isEmpty();
        // int result = name.length();
        // char result = name.charAt(0);// this returns the character present at the given index
        // int result = namw.indexOf("h");
        // String result = name.toLowerCase();
        // String result = name.trim(); // this trims the extra spaces at the beginnig and the end of string
        String result = name.replace("i", "I");
        System.out.println(result); 
    }
}
